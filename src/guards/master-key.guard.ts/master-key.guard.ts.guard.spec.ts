import { MasterKeyGuardTsGuard } from './master-key.guard.ts.guard';

describe('MasterKeyGuardTsGuard', () => {
  it('should be defined', () => {
    expect(new MasterKeyGuardTsGuard()).toBeDefined();
  });
});
